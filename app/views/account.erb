<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>"Welcome, <%= Helpers.current_user(session).username%>!"</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <h2>Your Balance:<%= Helpers.current_user(session).balance %> </h2>


    <a href="/sessions/logout">Log Out</a>

  </body>
</html>
